PORT=4000
DATABASE_URL=file:../database/prisma/dev.db
FRONTEND_URL=http://localhost:3000
ADMIN_EMAIL=admin@gmail.com
ADMIN_PASSWORD=admin@123
SMTP_ENABLED=false
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
SMTP_FROM="FoodBridge <no-reply@yourdomain.com>"
SMTP_REPLY_TO=support@yourdomain.com
OTP_TTL_MINUTES=10

# Demo-only: allow login with a shared password for existing users (do not use in real production).
DEMO_LOGIN_BYPASS=false
DEMO_LOGIN_PASSWORD= 932141
# Optional: comma-separated list of emails allowed to use demo bypass. Blank = allow all non-admin users.
DEMO_LOGIN_EMAIL_ALLOWLIST=
